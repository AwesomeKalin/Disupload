import * as http from 'http';

class httpServer{
    /*
    @Description The HTTP Server for Disupload's frontend
    */
}

export function start(port: number){
    console.log('Starting server on port ' + port);
}